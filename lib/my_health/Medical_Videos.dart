import 'package:flutter/material.dart';

class MedicalVideos extends StatelessWidget {
  const MedicalVideos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Medical Videos'),
      ),
    );
  }
}