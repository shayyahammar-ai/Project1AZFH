import 'package:flutter/material.dart';

class ScientificEncyclopedia extends StatelessWidget {
  const ScientificEncyclopedia({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('Scientific Encyclopedia'),
    );
  }
}