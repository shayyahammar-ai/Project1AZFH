import 'package:flutter/material.dart';

class PregnancyCalculator extends StatelessWidget {
  const PregnancyCalculator({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Pregnancy Calculator'),
      ),
    );
  }
}